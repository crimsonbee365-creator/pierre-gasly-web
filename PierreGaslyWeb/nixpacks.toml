# Pierre Gasly Backend - Railway Configuration
# Uses Supabase REST API (not direct database connection)
[phases.setup]
nixPkgs = [
  "php82",
  "php82Extensions.pdo",
  "php82Extensions.mbstring",
  "php82Extensions.curl"
]

[phases.install]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT"
